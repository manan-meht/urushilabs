# Pins verified by actually installing + importing on real Raspberry Pi OS
# (Debian trixie, aarch64, Python 3.13) hardware — see hardware/pi-client/README.md.
# aiortc==1.9.0's pinned `av<13.0.0` fails to build against this OS's FFmpeg 7.x
# (a channel-layout API rename) — aiortc>=1.10 relaxed that constraint and
# installs cleanly from prebuilt wheels with no compilation needed at all.
aiortc==1.15.0
av==17.1.0
aiohttp==3.11.11
sounddevice==0.5.1
numpy==2.2.1
python-dotenv==1.0.1
